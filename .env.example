# Database
# For production, use a managed PostgreSQL service like Neon, Supabase, or Vercel Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/sparrow"

# GROQ API
# Get your free API key from https://console.groq.com
# Free tier: 30 requests/min, 14,400 requests/day
GROQ_API_KEY="your-groq-api-key"

# NextAuth
# NEXTAUTH_URL should be your production URL in production (e.g., https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-generate-with-openssl-rand-base64-32"

# Cron Secret (for scheduled jobs)
# Generate a secret: openssl rand -base64 32
# Used to authenticate cron job requests
CRON_SECRET="your-cron-secret-key"

# Social Media APIs (Optional for Phase 3)
# Get these from respective developer platforms when ready to enable social posting
TWITTER_API_KEY=""
TWITTER_API_SECRET=""
LINKEDIN_CLIENT_ID=""
LINKEDIN_CLIENT_SECRET=""

# ========================================
# VERCEL FREE TIER NOTES
# ========================================
# - Cron jobs: Limited to 1 per day (already configured)
# - Daily cron runs at midnight UTC: /api/cron/daily-tasks
# - Processes RSS feeds, AI content analysis, and auto-posting
# - Rate limits configured to respect all API free tiers
